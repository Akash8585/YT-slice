import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "YT Slice - YouTube Playlist Duration Calculator",
  description: "Calculate the total duration of a selected range of videos from a YouTube playlist. Get playback speed estimates and detailed video information.",
  keywords: "YouTube, playlist, duration calculator, video time, playback speed",
  authors: [{ name: "YT Slice" }],
  openGraph: {
    title: "YT Slice - YouTube Playlist Duration Calculator",
    description: "Calculate the total duration of a selected range of videos from a YouTube playlist",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <head>
        <script 
          async 
          src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-pub-4887013408044651"
          crossOrigin="anonymous"
        />
      </head>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
